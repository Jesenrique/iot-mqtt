import { CambiarIconoDirective } from './cambiar-icono.directive';

describe('CambiarIconoDirective', () => {
  it('should create an instance', () => {
    const directive = new CambiarIconoDirective();
    expect(directive).toBeTruthy();
  });
});
